// Placeholder landing page. M5 replaces this with the agents overview. It exists
// now so the scaffold renders and the build has an entry route to compile.
export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>Overseer for Agents</h1>
      <p>Self-hosted observability for production LLM agents. Dashboard coming online.</p>
    </main>
  );
}
